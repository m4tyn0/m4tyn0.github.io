# /stack

tools and technologies i work with regularly.

## languages

    python mass;;;;;;;;; ██████████ daily driver
    c++    ;;;;;;;       ███████    professional exp
    javascript ;;;;;;    ██████     when needed
    typescript ;;;;      ████       growing
    bash   ;;;;;;        ██████     glue & automation

## frameworks & libraries

    fastapi      → apis, backends
    langchain    → llm orchestration  
    langgraph    → ai agents
    svelte       → frontend
    react        → frontend (when required)

## infrastructure

    docker       → containerization
    kubernetes   → orchestration (professional exp)
    linux        → servers, daily driver
    traefik      → reverse proxy
    nginx        → web server

## databases

    postgresql   → primary choice
    redis        → caching, queues
    mongodb      → when documents make sense
    timescaledb  → time-series data

## ai & ml

    openai api   → gpt integration
    anthropic    → claude integration
    open webui   → local llm interface
    ollama       → local models

## automation

    n8n          → workflow automation
    mcp servers  → llm tool integration
    custom apis  → whatever connects things

## other tools

    git          → version control
    uv           → python package management
    neovim       → editor
    obsidian     → notes (logseq before)